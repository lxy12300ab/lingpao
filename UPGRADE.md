# 群晖升级到 2026.09.20.6

适用于已部署 2026.09.20.2 的安装。本次数据库结构和运行依赖没有改变，仅覆盖 app/ 即可更新运行程序。

1. 在现有网页「偏好与管理」创建数据库备份，并下载到电脑保存。
2. 从 GitHub 下载本版本 ZIP，解压。保留旧程序文件副本以便回退。
3. 通过 File Station 将新版 app 文件夹复制到现有项目文件夹，选择合并并覆盖同名文件。
   不要将整个项目文件夹删除后重建。不要改动 data/、certs/、.env 或现有 docker-compose.yml。
4. 在群晖终端执行（此安装使用 root；其他用户可能需要 sudo）：

   ```bash
   cd /volume2/docker/lingpao-main
   sed -i 's/^BUILD=.*/BUILD=2026.09.20.6/' .env
   docker compose up -d --build
   docker compose ps
   curl http://127.0.0.1:5080/api/health
   ```

5. 确认健康接口中 build 为 2026.09.20.6，刷新 Safari 页面。
   若 iPhone 桌面仍保留旧图标，移除旧快捷图标后，通过 Safari 重新添加到主屏幕。

升级保留群晖现有数据、密钥、端口和证书，不会把电脑本地数据库自动同步到群晖。
不要再次运行 setup.py，不要覆盖 .env，不要删除 data。

本版包含倒序日/周/月/年图表、分页、日历手动修正、较大字号及零跑图标。
仓库的默认 compose 已移除部分群晖内核不支持的 CPU 配额；已手动移除此项的用户可以继续保留原 compose。

如果构建失败，先保存报错，不要删除容器数据或数据库。需要回退时，恢复升级前 app 文件夹和 BUILD 值，再重新构建；本版未变更数据库结构。
