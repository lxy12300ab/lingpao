# iPhone「记录里程」专用入口

2026.09.20.7 起支持：

- https://www.71zx.com:8443/upload
- 等效入口：https://www.71zx.com:8443/?action=upload

打开后自动显示上传界面；未登录先输入密钥，验证成功后继续进入上传。
普通首页仍显示统计。关闭上传后，定时刷新不会反复弹窗；重新访问入口或刷新入口页面可再次打开上传。
从系统后台返回不会清空已经选好的截图。

## 桌面快捷指令

1. 在 iPhone「快捷指令」新建快捷指令，命名「记录里程」。
2. 添加「URL」，填入上述 /upload 地址。
3. 添加「打开 URL」动作。
4. 在快捷指令详情中选择「添加到主屏幕」。
5. 点桌面图标进入上传界面，点选图片，核对截图实际拍摄日期，再开始识别。

不要把访问密钥加进 URL。Safari 和已有桌面 Web App 的登录状态可能不同，首次打开可能需要登录。
此快捷指令通过浏览器打开链接，不保证唤起已有的独立 Web App。

## 后续自动选图

网页不会在加载时强制打开相册选择器；通常需要网页内的一次用户点按。
后续可让快捷指令先运行「选择照片」，再将文件与实际截图日期提交到 POST /api/upload。
仅用「打开 URL」无法将快捷指令选择的照片自动填入网页文件输入框。
这属于下一阶段接口上传流程，本次尚未配置；不要将图片或密钥塞进 URL 参数。
